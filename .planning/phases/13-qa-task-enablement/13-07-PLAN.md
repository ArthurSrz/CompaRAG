---
phase: 13-qa-task-enablement
plan: 07
type: execute
wave: 7
depends_on: [13-04, 13-05, 13-06]
autonomous: false
requirements: []
---

<objective>
Surface the QA task end-to-end in the frontend: mode toggle (Benchmark | Sandbox), benchmark query picker, per-engine progress UI driven by SSE, retrieved-spans viewer, scored result reveal. The Chrome DevTools MCP loop validates the full path in production.

This plan is **not autonomous** — the production validation step requires a human operator (or an MCP-driven session) per `goal_directed_action_world/CLAUDE.md`.

Output: dual-mode arena UI, EventSource client, two new components (`ProgressCard`, `RetrievedSpansViewer`), Playwright spec, `13-VERIFICATION.md`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Re-enable 'qa' in taskTypes + add Mode toggle</name>
  <files>
    frontend/src/routes/tool-arena/components/ToolArenaForm.svelte
  </files>
  <action>
1. At `ToolArenaForm.svelte:32`, expand `type TaskType = 'summary'` → `type TaskType = 'summary' | 'qa'`. Add the `'qa'` entry to the `taskTypes` array (label, prompt, goalText come from i18n keys — add them in Task 2).

2. Add a `Mode = 'benchmark' | 'sandbox'` toggle as a DSFR radio group above the task-type select. Default `'sandbox'` for back-compat. **Only visible** when `selectedTaskType === 'qa'` (summary stays sandbox-only; benchmark queries are QA-only for v1).

3. Component state branch on mode:
   - `'sandbox'`: existing file upload + task + goal fields.
   - `'benchmark'`: hide file upload, hide goal field, show a `<select>` populated from `GET /tool-arena/eval-queries`. When user picks a query, the `task` and `goal` are read-only previews of the eval entry. Submit button enabled only when query selected.

4. Update the request payload:
   ```ts
   const body = {
     task_type: selectedTaskType,
     haystack: mode,
     ...(mode === 'sandbox'
       ? { task, goal, document_content: documentContent }
       : { evaluation_query_id: selectedEvalQueryId }
     ),
   }
   ```

5. Set the request to use SSE (see Task 3) — `Accept: text/event-stream`.
  </action>
  <verify>
    <automated>cd /Users/arthursarazin/Documents/ontology-worlds/goal_directed_action_world/CompaRAG/frontend && yarn lint 2>&1 | tail -10 && yarn check 2>&1 | tail -5</automated>
  </verify>
  <done>
    Lint clean. QA task type selectable. Mode toggle appears only for QA.
  </done>
</task>

<task type="auto">
  <name>Task 2: i18n keys for QA + Mode + progress states</name>
  <files>
    frontend/locales/fr.json
    frontend/locales/da.json
    frontend/locales/sv.json
    frontend/locales/et.json
    frontend/locales/lt.json
  </files>
  <action>
Add these keys to each locale (FR canonical; non-FR can use `[TODO-translate]` prefix):

```json
"toolArena.taskType.qa.label": "Question/Réponse",
"toolArena.taskType.qa.prompt": "Recherche la réponse à ta question dans le corpus.",
"toolArena.taskType.qa.goalText": "Réponse précise avec passages-source.",
"toolArena.mode.benchmark.label": "Évaluation",
"toolArena.mode.benchmark.description": "Question pré-définie sur le corpus partagé. Score automatique.",
"toolArena.mode.sandbox.label": "Bac à sable",
"toolArena.mode.sandbox.description": "Tes propres documents et questions. Vote utilisateur.",
"toolArena.evalQuery.placeholder": "Choisis une question",
"toolArena.progress.ingest_start": "Indexation du corpus...",
"toolArena.progress.ingest_progress": "Indexation : {processed}/{total} documents",
"toolArena.progress.ingest_done_cached": "✓ Indexation déjà en cache",
"toolArena.progress.ingest_done": "✓ Indexé en {took_ms}ms",
"toolArena.progress.retrieval_start": "Recherche des passages...",
"toolArena.progress.retrieval_done": "✓ {span_count} passages trouvés ({took_ms}ms)",
"toolArena.progress.mediation_start": "Rédaction de la réponse...",
"toolArena.progress.mediation_done": "✓ Réponse rédigée ({took_ms}ms)"
```

Inspect existing keys for casing/namespace conventions before adding.
  </action>
  <verify>
    <automated>cd /Users/arthursarazin/Documents/ontology-worlds/goal_directed_action_world/CompaRAG/frontend && for f in fr da sv et lt; do for k in "toolArena.taskType.qa.label" "toolArena.mode.benchmark.label" "toolArena.progress.retrieval_done"; do jq -e ".[\"$k\"]" "locales/$f.json" > /dev/null || echo "MISSING: $f / $k"; done; done; echo DONE</automated>
  </verify>
  <done>
    All keys present in all 5 locales.
  </done>
</task>

<task type="auto">
  <name>Task 3: SSE client + ProgressCard component</name>
  <files>
    frontend/src/routes/tool-arena/lib/sse-client.ts
    frontend/src/routes/tool-arena/components/ProgressCard.svelte
    frontend/src/routes/tool-arena/+page.svelte
  </files>
  <action>
1. `sse-client.ts` — a small wrapper around `fetch()` returning an async iterable of parsed event objects. Pattern:

```ts
export async function* streamCompare(body: ComparePayload): AsyncIterable<ToolArenaEvent> {
  const response = await fetch('/tool-arena/compare', {
    method: 'POST',
    headers: { 'Accept': 'text/event-stream', 'Content-Type': 'application/json' },
    body: JSON.stringify(body),
  })
  if (!response.body) throw new Error('No SSE body')
  const reader = response.body.getReader()
  const decoder = new TextDecoder()
  let buf = ''
  while (true) {
    const { value, done } = await reader.read()
    if (done) break
    buf += decoder.decode(value, { stream: true })
    let idx
    while ((idx = buf.indexOf('\n\n')) !== -1) {
      const chunk = buf.slice(0, idx)
      buf = buf.slice(idx + 2)
      const line = chunk.split('\n').find(l => l.startsWith('data: '))
      if (line) yield JSON.parse(line.slice(6))
    }
  }
}
```

Use `EventSource`? **No** — `EventSource` doesn't support POST bodies. The `fetch()` + ReadableStream pattern is what `backend/arena/streaming.py`'s SvelteKit consumer should also use (cross-check with the LLM duel's current implementation in `frontend/src/routes/duel/`).

2. `ProgressCard.svelte` — per-side card. Props: `side: 'a' | 'b'`, `event: ToolArenaEvent | null`. Renders:
   ```
   ┌─ Côté A ──────────────────────────────┐
   │ {currentStageLabel}                    │
   │ {optional progress bar for ingestion}  │
   └────────────────────────────────────────┘
   ```
   Uses i18n keys from Task 2. State transitions: `idle → ingesting → retrieving → mediating → complete | error`.

3. Update `+page.svelte` to drive the cards from the SSE stream:
   ```svelte
   {#each Array.from(events.a) as evt}
     <ProgressCard side="a" event={evt} />
   {/each}
   ```

   When the terminal `complete` event arrives, swap the cards for the existing result-pane component.
  </action>
  <verify>
    <automated>cd /Users/arthursarazin/Documents/ontology-worlds/goal_directed_action_world/CompaRAG/frontend && yarn lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    SSE client compiles. ProgressCard renders both states. Manual smoke test in `make dev-full` shows live stage transitions.
  </done>
</task>

<task type="auto">
  <name>Task 4: RetrievedSpansViewer + ScoreBadge components</name>
  <files>
    frontend/src/routes/tool-arena/components/RetrievedSpansViewer.svelte
    frontend/src/routes/tool-arena/components/ScoreBadge.svelte
  </files>
  <action>
1. `RetrievedSpansViewer.svelte` — collapsible per-side accordion. Lists each `RetrievedSpan` with rank, source_doc_id, char range, and (truncated) text. Behind the blind (only visible after reveal) so the comparison stays fair.

2. `ScoreBadge.svelte` — small pill showing `Recall@3: 1.0 · MRR: 0.5` when `judgement` is non-null. Only renders in benchmark mode. Render *before* reveal? Decision: render the badge **after reveal** alongside the tool name, so blind voting isn't biased by the automated score. This is a deliberate UX choice — surface the score-vs-human-vote contrast as a teaching moment.

3. Wire both into the result-pane component that already exists (`frontend/src/routes/tool-arena/components/ResultPane.svelte` or similar — verify exact name first).
  </action>
  <verify>
    <automated>cd /Users/arthursarazin/Documents/ontology-worlds/goal_directed_action_world/CompaRAG/frontend && yarn lint 2>&1 | tail -10 && yarn check 2>&1 | tail -5</automated>
  </verify>
  <done>
    Components compile. Spans viewer hidden before reveal. Score badge hidden in sandbox mode.
  </done>
</task>

<task type="auto">
  <name>Task 5: Playwright e2e for benchmark mode</name>
  <files>
    frontend/e2e/tool-arena-qa-benchmark.test.ts
  </files>
  <action>
Mirror `frontend/e2e/demo.test.ts` patterns. Test name: `"QA benchmark: pick eval query, watch progress, compare, vote, reveal score"`.

Steps:
1. Navigate to `/tool-arena`.
2. Select task type "Question/Réponse" from the dropdown (by label).
3. Click the "Évaluation" radio in the mode toggle.
4. Wait for the eval-query select to populate; pick `q01_capital_france` (or whichever id from the seed set).
5. Click "COMPARER LES OUTILS".
6. Wait for progress cards to appear; assert each transitions through `ingest_*`, `retrieval_*`, `mediation_*` (use accessible names from `toolArena.progress.*` i18n keys).
7. Wait for both result panes (up to 90s; benchmark is slower than sandbox because corpus is bigger).
8. Vote.
9. Click "RÉVÉLER LES OUTILS".
10. Assert each reveal card shows: tool name, latency, AND a `ScoreBadge` containing "Recall@" and "MRR".

Mark `test.slow()` and skip in default CI; set `PLAYWRIGHT_BACKEND=production` env var to enable.
  </action>
  <verify>
    <automated>cd /Users/arthursarazin/Documents/ontology-worlds/goal_directed_action_world/CompaRAG/frontend && PLAYWRIGHT_BACKEND=local yarn playwright test e2e/tool-arena-qa-benchmark.test.ts --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <done>
    Test passes against `make dev-full`.
  </done>
</task>

<task type="manual">
  <name>Task 6: Production Chrome DevTools MCP validation + write 13-VERIFICATION.md</name>
  <files>
    .planning/phases/13-qa-task-enablement/13-VERIFICATION.md
  </files>
  <action>
After plans 13-01..13-06 are merged + deployed (backend auto-deploys; `rag-pill` requires manual `railway up --service rag-pill` per `CompaRAG/CLAUDE.md`):

Run TWO Chrome DevTools MCP loops — one per mode.

**Loop 1: Benchmark mode**
1. Navigate to `https://comparag.vercel.app/tool-arena`.
2. Select QA + Évaluation. Pick `q01_capital_france`.
3. Click compare. Observe progress cards live (screenshot mid-stream).
4. Wait for terminal `complete`. Screenshot both result panes.
5. Vote. Reveal. Screenshot the score badges.
6. `list_network_requests` + `get_network_request` on the `/tool-arena/compare` SSE call. Save the event sequence to verification file.

**Loop 2: Sandbox mode**
1. Same nav. Select QA + Bac à sable.
2. Upload `scripts/fixtures/qa_capital_paris.txt`.
3. Enter question: "Quelle est la capitale mentionnée ?"
4. Click compare. Observe progress, panes, vote, reveal.
5. Assert score badges are absent (sandbox has no ground truth).

`13-VERIFICATION.md` captures:
- Timestamp.
- Screenshot paths.
- Verbatim event sequence per side (benchmark).
- Score badges' content per side (benchmark).
- Latency comparison: which engine finished ingestion first? Which retrieved fastest?
- Was the "needle was found" claim consistent between the LLM mediated answer and the structured retrieval score? (I.e., did the LLM say "the answer is X" while the score said `contains_gold=False`? That's a future investigation hook.)
- Any UX issues encountered (progress jank, layout shift, missing translations).

If anything fails (timeout, ERROR pane, no score badge in benchmark): document and open `13-08-PLAN-fix-{symptom}.md`.

Update `.planning/STATE.md` to mark Phase 13 complete (or partial with follow-up plan listed).
  </action>
  <verify>
    Manual: human operator (or Claude session with Chrome DevTools MCP) signs off `13-VERIFICATION.md` with both loops documented.
  </verify>
  <done>
    Both modes round-trip in production. Score badges visible in benchmark mode, hidden in sandbox. Progress events arrive live, not in a final burst. STATE.md reflects phase completion.
  </done>
</task>

</tasks>
