---
phase: 13-qa-task-enablement
plan: 04
type: execute
wave: 4
depends_on: [13-03]
autonomous: false
requirements: []
---

<objective>
Re-enable QA in the frontend task-type picker and prove the full path works in production via the Chrome DevTools MCP loop. The backend, dispatcher, and mediator are in place after plans 13-01 through 13-03 — this plan is the gate that flips the UI back on.

This plan is **not autonomous**: it includes a manual E2E validation step in production, plus a deploy step for `rag-pill` (manual per `CLAUDE.md`).

Output: `qa` re-added to the frontend `taskTypes` array with i18n strings in all 5 locales; one new Playwright e2e spec; the documented Chrome DevTools loop run by an operator and the result captured in `13-VERIFICATION.md`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add 'qa' entry to taskTypes array</name>
  <files>
    frontend/src/routes/tool-arena/components/ToolArenaForm.svelte
  </files>
  <action>
At `frontend/src/routes/tool-arena/components/ToolArenaForm.svelte:34` (the `taskTypes` array), append:

```ts
{
  value: 'qa',
  label: m['toolArena.taskType.qa.label'](),
  prompt: m['toolArena.taskType.qa.prompt'](),
  goalText: m['toolArena.taskType.qa.goalText'](),
}
```

This makes `taskTypes.length === 2`, which trips the `{#if taskTypes.length > 1}` guard added earlier (commit `df3b5a6a`) so the dropdown surfaces again.

Remove the stale "// Currently only summary is exposed" comment block (lines ~27-31).

Keep the `// Mirrors backend/tool_arena/task_kind.py — keep in sync.` comment from plan 13-02.
  </action>
  <verify>
    <automated>cd /Users/arthursarazin/Documents/ontology-worlds/goal_directed_action_world/CompaRAG/frontend && yarn lint 2>&1 | tail -10 && yarn check 2>&1 | tail -5</automated>
  </verify>
  <done>
    Lint clean. Both 'summary' and 'qa' entries present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add i18n strings for QA in all 5 locales</name>
  <files>
    frontend/locales/fr.json
    frontend/locales/da.json
    frontend/locales/sv.json
    frontend/locales/et.json
    frontend/locales/lt.json
  </files>
  <action>
Add three keys under the existing `toolArena.taskType` group in each locale file. Use these French strings as the canonical version and translate (or copy-with-TODO for non-FR locales — the project supports `fr` as primary; other locales can mirror until properly translated):

`fr.json`:
```json
"toolArena.taskType.qa.label": "Question/Réponse",
"toolArena.taskType.qa.prompt": "Pose une question précise sur le document.",
"toolArena.taskType.qa.goalText": "Réponse exacte avec citations des passages pertinents."
```

For `da.json`, `sv.json`, `et.json`, `lt.json`: if you don't speak the language, write the English equivalent with a `// TODO: translate` comment in the matching place (Paraglide JSON doesn't accept comments — instead, prefix the value with `[TODO-translate] `). Keep the keys identical to the FR ones.

Inspect existing `toolArena.taskType.summary.*` entries first to match casing and structure exactly.
  </action>
  <verify>
    <automated>cd /Users/arthursarazin/Documents/ontology-worlds/goal_directed_action_world/CompaRAG/frontend && for f in fr da sv et lt; do jq -e '."toolArena.taskType.qa.label"' "locales/$f.json" > /dev/null || echo "MISSING in $f"; done; echo "DONE"</automated>
  </verify>
  <done>
    All 5 locale files contain the three QA i18n keys. `jq -e` finds each.
  </done>
</task>

<task type="auto">
  <name>Task 3: Playwright spec for QA happy path</name>
  <files>
    frontend/e2e/tool-arena-qa.test.ts
  </files>
  <action>
Mirror `frontend/e2e/demo.test.ts` patterns. Test name: `"QA task: upload doc, ask question, compare, vote, reveal"`.

Steps:
1. `page.goto('/tool-arena')`
2. Select the file input, upload a small fixture .txt with known content (use `scripts/fixtures/` per recent additions in git status).
3. Pick `Question/Réponse` from the task-type dropdown (use the label, not the value, so locale changes don't break the test).
4. Fill the task field: `"What is the capital city mentioned in the document?"`
5. Click the goal field once (per the documented submit-button-stays-disabled-until-touched gotcha in `goal_directed_action_world/CLAUDE.md`).
6. Click "COMPARER LES OUTILS".
7. Wait up to 60s for both result panes to render (selectors: `[data-testid="result-a"]`, `[data-testid="result-b"]` — confirm these IDs exist in the existing component, add them if not).
8. Assert neither pane shows "Tool encountered an error".
9. Vote (radio button or `evaluate_script` fallback per the documented loop).
10. Click "RÉVÉLER LES OUTILS".
11. Assert both reveal cards show a tool name (one of `qa_precise`, `qa_broad`).

If the fixture file doesn't exist yet, create a minimal one at `scripts/fixtures/qa_capital_paris.txt` containing one sentence: `"Paris is the capital city of France and home to the Eiffel Tower."`

This test is slow (real backend round-trip via the deployed `comparag.vercel.app` or `localhost:5173` with `make dev-full`). Mark it with `test.slow()` and skip in CI by default unless `PLAYWRIGHT_BACKEND=production` is set.
  </action>
  <verify>
    <automated>cd /Users/arthursarazin/Documents/ontology-worlds/goal_directed_action_world/CompaRAG/frontend && yarn playwright test e2e/tool-arena-qa.test.ts --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <done>
    Test passes against a running stack (local `make dev-full` or production).
  </done>
</task>

<task type="manual">
  <name>Task 4: Production Chrome DevTools MCP validation + write 13-VERIFICATION.md</name>
  <files>
    .planning/phases/13-qa-task-enablement/13-VERIFICATION.md
  </files>
  <action>
After plans 13-01..13-03 are merged and deployed (backend auto-deploys; `rag-pill` requires `railway up --service rag-pill` per `CLAUDE.md`):

Run the Chrome DevTools MCP loop documented in `goal_directed_action_world/CLAUDE.md`:

1. Navigate to `https://comparag.vercel.app/tool-arena`.
2. Verify the task-type dropdown now shows both "Résumé" and "Question/Réponse".
3. Upload `scripts/fixtures/qa_capital_paris.txt`.
4. Select "Question/Réponse".
5. Enter task: `"Quelle est la capitale mentionnée dans le document ?"`
6. Click the goal field, click "COMPARER LES OUTILS".
7. Wait ~30s.
8. Verify both panes show content (one should contain "Paris" or `[s1]`-style citations; refusal phrase is acceptable for one of the two pills if it failed retrieval).
9. Vote via `evaluate_script` (`document.getElementById('tool-radio-b').click()`).
10. Click "RÉVÉLER LES OUTILS".
11. Verify reveal shows `qa_precise` and `qa_broad` (in some order) as the two tools.
12. Use `list_network_requests` + `get_network_request` to capture the `/tool-arena/compare` response body.

Write `13-VERIFICATION.md` containing:
- Timestamp of run.
- Screenshot reference paths (capture via `take_screenshot`).
- Both panes' mediated_result text (verbatim).
- Whether the QA refusal phrase appeared (and if so, in which pane).
- Network call latency for each tool from `MCPToolCall.duration_ms`.
- Any deviations from the expected flow.

If anything fails (timeout, ERROR pane, wrong tool revealed, missing fields): document the failure mode and open a follow-up plan (`13-05-PLAN-fix-{symptom}.md`) rather than retry-looping.
  </action>
  <verify>
    Manual: a human operator (or a Claude session with Chrome DevTools MCP access) runs the loop and signs off `13-VERIFICATION.md`.
  </verify>
  <done>
    `13-VERIFICATION.md` exists with timestamped evidence. The arena round-trips QA successfully end-to-end in production. STATE.md updated to mark Phase 13 complete.
  </done>
</task>

</tasks>
