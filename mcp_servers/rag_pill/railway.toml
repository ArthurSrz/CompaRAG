[build]
builder = "DOCKERFILE"
dockerfilePath = "mcp_servers/rag_pill/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
